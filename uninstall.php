<?php
/**
 * فایل حذف افزونه (Uninstall)
 * این فایل زمانی اجرا می‌شود که کاربر افزونه را به طور کامل حذف کند.
 */

// ۱. اگر عملیات حذف توسط وردپرس صدا زده نشده باشد، خروج امن
if (!defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}

/**
 * ۲. پاکسازی تنظیمات ذخیره شده در جدول options دیتابیس
 */
delete_option('isp_settings');

/**
 * ۳. پاکسازی داده‌های احتمالی دیگر (در صورت نیاز در فازهای آینده اینجا تکمیل می‌شود)
 */
// مثال: حذف جداول اختصاصی یا کش‌های موقت